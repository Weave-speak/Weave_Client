{
  "name": "weave-client",
  "version": "0.1.0",
  "description": "Weave client — one core, three modes",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "dependencies": {
    "mediasoup-client": "^3.22.0"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
